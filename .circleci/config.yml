version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - npm/install:
        command: npm install
      - cypress/run:
          command: npx cypress run --headless --component
